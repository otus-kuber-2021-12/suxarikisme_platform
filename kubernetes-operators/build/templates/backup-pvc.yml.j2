apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "backup-{{ name }}-pvc"
spec:
  accessModes:
    - "ReadWriteOnce"
  storageClassName: ""
  resources:
    requests:
      storage: {{ storage_size }}
