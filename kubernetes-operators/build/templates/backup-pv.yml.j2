apiVersion: v1
kind: PersistentVolume
metadata:
  name:  "backup-{{ name }}-pv"
  labels:
    pv-usage: "backup-{{ name }}"
spec:
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 1Gi
  hostPath:
    path: /data/pv-backup/
